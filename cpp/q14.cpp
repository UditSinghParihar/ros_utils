#include <bits/stdc++.h>

using namespace std;

/*
Using constant O(1) copy elements from vector to a set
*/

int main(int argc, char const *argv[]){
	string a = "Jen";

	// append lenght of a in a
	a += to_string(a.length());

	fprintf(stderr, "%s\n", a.c_str());


	// map with repeated keys allowed

	return 0;    

}