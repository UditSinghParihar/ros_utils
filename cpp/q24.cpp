#include <bits/stdc++.h>

using namespace std;


int main(int argc, char const *argv[]){
	string str1("dd");
	printf("%s %lu %lu\n", str1.c_str(), str1.length(), str1.size());

	// string str1(" hello world ");

	// stringstream ss(str1);
	// string str2;

	// while(ss >> str2){
	// 	fprintf(stderr, "%s %lu\n", str2.c_str(), str2.length());
	// }


	return 0;
}