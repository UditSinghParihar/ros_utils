#include <bits/stdc++.h>

using namespace std;


int main(int argc, char const *argv[]){
	int a=0, b=0, c=0, d=0;

	cin >> a >> b;
	cin >> c;
	// scanf("%d %d %d", &a, &b, &c);

	printf("a: %d; b: %d; c: %d\n", a, b, c);

	return 0;
}